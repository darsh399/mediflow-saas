// Placeholder auth validator (e.g., using Joi or Zod)

export const validateLogin = (data) => {
  // validate credentials
  return { valid: true, errors: null };
};

export default { validateLogin };
